server:
  port: 0 # Random port for web, we won't use it much

spring:
  application:
    name: fx-service
  data:
    redis:
      password: ${REDIS_PASSWORD:}
      cluster:
        nodes: ${REDIS_CLUSTER_NODES:${REDIS_HOST:redis}:${REDIS_PORT:6379}}

grpc:
  server:
    port: 9091
